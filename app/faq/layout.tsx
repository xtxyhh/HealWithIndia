import { ReactNode } from "react";

export const metadata = {
  title: "FAQ | HealWithIndia",
  description:
    "Frequently asked questions about medical tourism, hospitals, treatment costs and healthcare in India.",
};

export default function FAQLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}